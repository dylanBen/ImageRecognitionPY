# ImageRecognitionPY Logistic Regression

* Requirement : python & tensorflow (native or docker) with jupyter server

    - pip install pandas
    - pip install numpy
    - pip install sklearn
    - pip install pillow
    - pip install seaborn

* If Docker Install

    - pip install keras
    - pip install pillow
    
